add_executable(sdk_tests sdk_tests.cpp)
target_link_libraries(sdk_tests PRIVATE naval_sdk::naval_sdk)
target_compile_definitions(sdk_tests PRIVATE FIXTURE_DIR="${CMAKE_CURRENT_SOURCE_DIR}/fixtures")
foreach(suite protocol lifecycle tactical recording cli parity)
    add_test(NAME ${suite} COMMAND sdk_tests ${suite})
endforeach()
add_executable(transport_client transport_client.cpp)
target_link_libraries(transport_client PRIVATE naval_sdk::naval_sdk)
find_package(Python3 COMPONENTS Interpreter QUIET)
if(Python3_FOUND)
    execute_process(COMMAND ${Python3_EXECUTABLE} -c "import websockets"
        RESULT_VARIABLE HAVE_WEBSOCKETS OUTPUT_QUIET ERROR_QUIET)
    if(HAVE_WEBSOCKETS EQUAL 0)
        add_test(NAME transport COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/transport_test.py $<TARGET_FILE:transport_client>)
        set_tests_properties(transport PROPERTIES TIMEOUT 60)
    endif()
    if(DEFINED ENV{BATTLE_SIM_SERVER})
        add_test(NAME rust_server COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/rust_server_test.py $<TARGET_FILE:transport_client>)
        set_tests_properties(rust_server PROPERTIES TIMEOUT 60)
    endif()
endif()
